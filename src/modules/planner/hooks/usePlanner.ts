/**
 * @fileoverview Placeholder for the removed legacy planner hook. 
 * Redirection or basic state can be managed here if needed.
 */
'use client';

export const usePlanner = () => {
    return {
        state: { isLoading: false },
        actions: {},
        selectors: {},
        isAuthorized: true
    };
};
